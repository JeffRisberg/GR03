package com.incra.session

import com.incra.Account 

/**
 * The <i>AccountSession</i> holds state information used by the AccountController.
 * Nothing a the moment, but perhaps the filter state will go here.
 * 
 * @author Jeff Risberg
 * @since 11/20/10
 */
class AccountSession {
  Account currentAccount // not currently used
  
  /** Constructor */
  AccountSession() {
  }
}
